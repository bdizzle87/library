let myLibrary = [];

function Book(title, author, pages) {
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.read = read;
    this.info = function() {
        console.log(name + "by" + author + ", " + pages + " pages, not read yet.");
    }
}

function addBookToLibrary() {
  // do stuff here
}

function displayBooks() {
    
}